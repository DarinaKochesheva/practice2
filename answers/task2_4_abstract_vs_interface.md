# Задание 2.4 — Абстрактный класс vs Интерфейс

## Теоретическое напоминание

| Критерий | Абстрактный класс | Интерфейс |
|----------|-------------------|-----------|
| Отношение | «является» (is-a) | «умеет» (can-do) |
| Наследование | Одиночное (`extends`) | Множественное (`implements`) |
| Поля (состояние) | Могут быть любые | Только `static final` |
| Конструктор | Есть | Нет |
| Методы с реализацией | Да | `default`/`static` (с Java 8) |

**Правило выбора:**
- Если есть общие **поля** (состояние) и отношение «является» → **абстрактный класс**.
- Если нужна **множественная реализация** или отношение «умеет» → **интерфейс**.

## Ваша таблица

| Ситуация                                                                           | Ваш выбор         | Обоснование                                                                                                                                                           |
|------------------------------------------------------------------------------------|-------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Все животные имеют имя и возраст, но звучат по-разному                             | Абстрактный класс | Есть общее состояние (имя, возраст) — поля. Звук разный - абстрактный метод makeSound()                                                                               |
| Некоторые объекты (документ, изображение, настройки) можно сериализовать           | Интерфейс         | Отношение «умеет» — разные по природе объекты могут поддерживать одну способность. Множественная реализация: один класс может реализовать несколько интерфейсов       |
| Все транспортные средства имеют скорость и топливо, но двигаются по-разному        | Абстрактный класс | Есть общее состояние (скорость, уровень топлива). Разные способы движения - абстрактный метод move()                                                                  |
| Робот, посудомоечная машина и телефон — все умеют подключаться к Wi-Fi             | Интерфейс         | Отношение «умеет». Разные по природе объекты имеют общую способность. Множественная реализация важна: робот может и двигаться, и подключаться к Wi-Fi                 |
| Круг, квадрат и треугольник — все фигуры с площадью и периметром, у всех есть цвет | Абстрактный класс | Есть общее состояние (цвет). Площадь и периметр вычисляются по-разному - абстрактные методы                                                                           |
| Класс должен одновременно уметь летать, плавать и ездить                           | Интерфейс         | Нужна множественная реализация — один класс не может наследовать несколько абстрактных классов. Лучше реализовать несколько интерфейсов: Flyable, Swimmable, Drivable |
